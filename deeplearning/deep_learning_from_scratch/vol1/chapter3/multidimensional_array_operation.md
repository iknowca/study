# Multidimensional Array Operation

In deeplearning, multidimensional array computations are commonly used.

## Multidimensional Array

A multidimensional array is an array that has more than one dimension.
```python
import numpy as np

# 1D array
A = np.array([1, 2, 3, 4])
# 2D array
B = np.array([[1, 2], [3, 4], [5, 6]])
```

## matrix multiplication

Matrix multiplication is a common operation in deep learning. The matrix multiplication of two matrices A and B is calculated by the following formula:
There is 2 features in matrix multiplication. The first feature is the number of columns of the first matrix must be equal to the number of rows of the second matrix. The second feature is the shape of the resulting matrix is the number of rows of the first matrix and the number of columns of the second matrix.
![matrix_multiplication](./img/matrix_mutiplication/main_ManimCE_v0.18.1.gif)


![matrix_multiplication](./img/matrix_multiplication2/main_ManimCE_v0.18.1.gif)



